(function() {

  var locales = {
    'home': {
      'slide0': {
        'title': 'We are Etéreo'
      }
    }
  };

  // i18njs.add(language, [namespace,] locales);
  i18njs.add('en', locales);

})();
